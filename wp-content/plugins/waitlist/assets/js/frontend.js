(function($){
	// Placeholder frontend bundle. Real ES6 modules can be built into this file later.
	$(function(){
		// Namespace guard
		window.TFIBWaitlist = window.TFIBWaitlist || {};
		// console.log('TFIB Waitlist frontend loaded');
	});
})(jQuery);
